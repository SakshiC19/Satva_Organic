# Disable ESLint plugin during build
DISABLE_ESLINT_PLUGIN=true
# Don't treat warnings as errors
CI=false
# Disable source maps in production
GENERATE_SOURCEMAP=false

# TPC Credentials
TPC_CLIENT_ID=JSPSAT
TPC_PASSWORD=2026@SATVA
